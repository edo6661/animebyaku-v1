// ! last patch
const UseReview = () => {
    // const { data } = useQuery({
    //     queryKey: ["reviews", mal_id],
    //     queryFn: () => getRequestAnimeReview
    // })
}

export default UseReview